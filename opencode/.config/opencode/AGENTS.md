## Git

- When at all possible, if interacting with Github, use the gh CLI over using raw git commands.
- Always prefer the starting of new work/plans by creating a new branch within a new worktree, and the storage of the worktree should be within a `.worktrees` directory inside the project context you're working in.

