-- Personal input settings migrated from hyprland_custom.conf for Omarchy Quattro.

hl.config({
  input = {
    numlock_by_default = true,
    repeat_delay = 200,
    repeat_rate = 40,
    follow_mouse = 1,
    off_window_axis_events = 2,
    touchpad = {
      natural_scroll = false,
      disable_while_typing = true,
      clickfinger_behavior = true,
      tap_to_click = false,
      scroll_factor = 0.4,
    },
  },
})
